{
  "name": "amtool",
  "version": "0.0.9",
  "license": "MIT",
  "type": "module",
  "bin": {
    "amtool": "bin/amtool.js"
  },
  "files": [
    "lib",
    "bin"
  ],
  "scripts": {
    "build": "rm -rf lib && tsc",
    "watch": "rm -rf lib && tsc -w",
    "prepublish": "npm run build",
    "test": "echo \"sure would be nice to have some tests!\" && exit 1"
  },
  "dependencies": {
    "@automerge/automerge-repo": "^1.1.1",
    "@automerge/automerge-repo-network-websocket": "^1.1.1",
    "chokidar": "^3.6.0",
    "cmd-ts": "^0.13.0"
  },
  "devDependencies": {
    "@types/chokidar": "^2.1.3",
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  }
}
